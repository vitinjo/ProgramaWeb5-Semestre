//Aspa simples (') apenas um parâmetro. Aspas duplas (") podemos passar mais de um parâmetro.

  var nm=prompt("Digite um nome");
//Aparece apenas no console do navegador
  console.log("O nome digitado foi" +nm);

//Aparece no navegador para o usuário
  document.write("O nome digitado foi " +nm);